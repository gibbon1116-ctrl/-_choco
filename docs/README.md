# ブラウザ版「勤務表メーカー」

このフォルダはドキュメント置き場ではなく、ブラウザで動くアプリ本体です。

フォルダ名が `docs` なのは、GitHub Pagesの「Deploy from a branch」機能で公開元として選べる場所が `/ (root)` または `/docs` に限られているためです。

## 起動方法

GitHub Pagesで公開されたURLへアクセスするだけで起動できます。アプリは今後のフェーズで順次書き換えていくため、詳しい利用方法は後続フェーズで追記する予定です。
